export interface QuizQuestion {
  question: string;
  noResponse: string;
  yesResponse: string;
  yesButton: string;  // Text for the first button
  noButton: string;   // Text for the second button
}